# é importante lembrar que em casos onde há o uso de migrations, não precisamos
# especificar dentro dos models os campos que ele possui, normalmente apenas
# sobrescrevemos alguns métodos e criamos nóvos para o model
class Kind < ApplicationRecord
end
